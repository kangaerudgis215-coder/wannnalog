# CLAUDE.md — WannaLog

## このアプリについて
- WannaLog は、「したい」（食べたい・行きたい・見たい・欲しい・やりたい・知りたい…）を**出た瞬間に保存**し、AIで自動整理し、**通知で思い出させ**、達成チェックと**ビジョンボード**で“夢を可視化”する iOS アプリ。
- 「保存して終わり」で終わらせず、**行動（達成）まで運ぶ**のが核。敵は「忘却」。
- 詳しい設計は `docs/` を参照（特に `docs/product-vision.md`、`docs/stage2-design/`）。
- オーナー（開発者）はプログラミング初心者。専門用語は最小限にして、やさしい日本語でやり取りする。
- 報告・説明はすべて日本語で行う。コード内のコメントも日本語でよい。

## いまの開発環境（重要）
- 開発は Claude Code（ウェブ／リモート）で行う。オーナーは **Windows** を使用していて、**Mac はまだ無い**。
- 技術スタックは **React Native + Expo**。
  - 理由：**Mac 無しの Windows でも開発・配信できる**ため。Swift/SwiftUI は Mac（Xcode）必須なので採用しない。
- Expo SDK は **54**（App Store の Expo Go が対応する版に合わせる）。**SDK を上げるときは必ず Expo Go の対応状況を先に確認**する（上げすぎると実機で開けなくなる）。
- **Node は必ず LTS（v20 か v22）**を使う。最新の非 LTS（v24 等）は Expo の道具が未対応でエラーになる。
- 実機確認は **iPhone の Expo Go アプリ**で QR を読む。配信が必要になったら **EAS Build（クラウド・Mac不要）**。Apple Developer 登録（$99/年）は配信時に。
- → コードの正しさに、いつも以上に注意する。ロジックは小さく分け、テストで検証できる形にする。
- → セットアップ／実行手順は `app/README.md` と `docs/` に書き残す。

## 進め方（オーナーはなるべく操作せず、たまに見るだけ）
- 基本は自分で判断して進める。小さな選択でいちいち止まらない。判断したら「何を・なぜそう決めたか」を一言だけ残す。
- 質問は本当に必要なときだけ。しかも、まとめて1回で聞く（細かい確認の連発はしない）。
- 取り返しのつかない操作は、勝手にやらず必ず先に確認する。具体的には：
  データの削除 / アプリ名・バンドルIDの変更 / 有料の依存ライブラリの追加 / 外部サービスとの連携 / お金や公開が絡む操作。
- 作業は小さな単位で進める：実装 → テスト → わかりやすい日本語メッセージでコミット →（必要なら）PR。
- どの時点でも、リポジトリが「壊れていない状態」を保つ。

## 報告のしかた（ここが一番大事）
ひと区切りごとに、次の形で「やさしい日本語の短い報告」を残す：
- ✅ やったこと：（1〜3行、専門用語なし）
- 🔜 次にやること：（1行）
- ❓ オーナーに頼みたいこと：（あれば書く。なければ「なし」）

むずかしい言葉を使うときは、かならず一言で意味を添える。

## コードの決まり
- ファイルや関数は小さく、名前はわかりやすく。
- 新しいロジックには、できるだけテストを書く。
- 変更はこまめにコミットする。メッセージは「何をしたか」が一目でわかる日本語で。
- 迷ったら「シンプルで読みやすい」ほうを選ぶ。

## チーム運用（このプロジェクト特有）
- 秘書がオーナーの窓口。専門家エージェント（専属批判家・市場調査・マネタイズ・デザイン・SNSマーケ）を必要なときに起動し、議論・批判の上で要点をまとめて報告し、承認を得てから進める。
- 各成果物は `docs/` に残し、コミットして保全する（実行環境は一時的なため）。

## このファイルについて
- プロジェクトの方針が変わったら、この CLAUDE.md も更新する。
- ここに書いてあることは、毎回の作業の前提として必ず守る。
