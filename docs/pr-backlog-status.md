# 未マージPRの整理状況（2026-08-21 更新）

自動セッションは「App.jsから純粋関数を切り出してテストを足す」整理PRを日々作っており、これは**見た目・動作を変えない安全な変更**なので、以降は都度オーナーの確認を待たずに直接マージしてよい運用にしている。新機能・UI変更を含むPRは、実機（Expo Go）での確認が必要なため、引き続きオーナー判断待ちとして残す。

## これまでの経緯（要約）

- 2026-07-29：#39・#7 をマージ、重複PR（#34〜#36）をクローズ。
- 2026-07-31：#41（マイページ集計ロジックの分離）をマージ、重複の#3をクローズ。
- 2026-08-07：#44/#45/#46/#47（通知振り分け・ハッシュ計算・並び順・連続達成日数の分離）をマージ。

## 2026-08-13（今回）

このリストに同種の整理PR（#50〜#54）が5件たまっていたため、中身を確認（すべて見た目・動作を変えない純粋ロジックの切り出し＋テスト追加）した上で、まとめて直接マージした。

- **マージ**：#52（`theme.js` にテスト追加。コード変更なし）
- **マージ**：#54（文字太さ→フォント選択ロジックを `font.js` へ）
- **マージ**：#53（ビジョンボードの字体・進み具合タグを `vision.js` へ）※ #54とのApp.js import行の衝突を手動で解消
- **マージ**：#50（マイページのカテゴリ別達成集計を `stats.js` へ）
- **マージ**：#51（ホームの表示フィルタを `filter.js` へ）※ #50とのApp.js import行の衝突を手動で解消
- **クローズ**：#49（今回の内容に統合済みの古いdocs更新PR）
- 追加で、`notify.js` の `nextRemindAt`（毎週リマインドの分岐）と `remindSummary`（日時指定の表示）にテストが無い分岐を見つけたので、テストを追加（新規PR）。
- 結果：`npx jest` は 20 suites / 160 tests すべて成功（作業前は155 tests）。オープンPRは15件→10件に減少。

これで **App.js から切り出せる「見た目を変えない純粋ロジック」はほぼやり切った**。次にこの種の整理PRを思いつく場合は、まず `app/*.js`（App.js以外）にテストの薄い関数が残っていないか確認してから着手する。

## 2026-08-17（今回）

- **マージ**：#56（`sns.js` の `parseSnsLink()` で YouTube 以外のドメインを誤ってYouTubeと判定するバグ修正。見た目・動作を変えない安全な修正のため直接マージ）。
- 同じ観点で `sns.js` の X/Twitter 判定を見直したところ、`twitter.com` には `host.endsWith('.twitter.com')` のサブドメイン対応があるのに、`x.com` には対応する `.x.com` のサブドメイン判定が無い抜けを発見。`mobile.x.com` のようなURLだと「対応外リンク」扱いになってしまっていた。他プラットフォーム（Instagram/TikTok）と同じ形に揃えて修正し、回帰テストを追加（新規PR）。
- 結果：`npx jest` は 20 suites / 177 tests すべて成功（作業前は176 tests）。

## 2026-08-20（今回）

- 前回までに溜まっていた整理PR3件（#58 x.comサブドメイン判定の修正、#59 ogp.jsのテスト追加、#60 reorder/garden/heat/sns/affiliate/filter.jsのテスト追加）はすべて見た目・動作を変えない安全な変更だったため、直接マージした。
- マージ後にカバレッジを見直したところ、`ogp.js` にまだ分岐カバレッジの抜け（baseUrl省略時・res.url省略時・URL未指定時などの防御的な分岐）が残っていたので、テストを追加。`extractPlaceFromUrl` の catch 分岐で結果が空文字になるケース（line102）は、正規表現の仕様上 `%` を含む文字は最終的な文字列に残るため実質到達不可能と判断し、対象外とした（garden.js/sns.js で既に同様の判断をしている前例に合わせた）。
- 結果：`npx jest` は 20 suites / 195 tests すべて成功（マージ直後は187 tests）。`ogp.js` は statements/branches/functions/lines ほぼ100%（branch 98.55%、残り1分岐は上記の理由で対象外）。

## 2026-08-21（今回）

- 前回（#61）までの整理PRをマージ。カバレッジを見直したが、`garden.js`（span<=0）・`sns.js`（`hostOf`のurl未指定時）・`ogp.js`の残り分岐は、いずれも実際の呼び出し経路からは到達できない防御的コードと判明したため対象外（前回までの判断を踏襲）。
- 代わりにコード全体を見直したところ、`links.js` の `browserUrl()`（Chrome/Safariのブラウザ選択用）が、2026-08-10のコミットでApp.js側のブラウザ選択機能自体を廃止した際に消し忘れられ、どこからも呼ばれない死んだコードとして残っていることを発見。関数本体と専用テスト（`browser.test.js`）を削除した（見た目・動作の変化なし。UIからは既に無かった機能なので実質的な影響はゼロ）。
- 結果：`npx jest` は 19 suites / 191 tests すべて成功（作業前は20 suites / 195 tests。使われていない機能のテストを1ファイル削除したため件数が減少）。

## 2026-08-25（今回）

- 前回（#61）以降に溜まっていた整理PR #64（ビジョンボードの表紙・棚分けロジック`buildVisionBoard`を`vision.js`へ分離）を確認したところ、見た目・動作を変えない安全な変更だったため直接マージした。
- App.js内でまだ切り出されていない純粋ロジックを探し、詳細画面（`DetailScreen`）の中にあった「保存元リンク（あれば）＋カテゴリ別アクションリンク」を組み立てる処理を発見。`links.js`に`detailLinks()`として切り出し、テストを3件追加した（新規PR #65。今回はPR作成までとし、マージは次回セッションで判断）。
- 結果：`npx jest` は 19 suites / 199 tests すべて成功（作業開始時は196 tests）。`links.js` は statements/branches/functions/lines すべて100%。
- App.js以外の純粋ロジック抽出は、これでほぼ出尽くした感触（既存モジュールはどれも100%近いカバレッジ）。次回以降は、App.js内にまだ残っている小さな計算ロジックが無いか探しつつ、そろそろ新機能系PR（特に#30/#63のキーワード検索）をオーナーに実機確認してもらうタイミングかもしれない。

## 2026-08-27（今回）

- 前回（8/25）以降に溜まっていた整理PR2件（#65 詳細画面のリンク組み立て`detailLinks`、#66 保存アイテムの組み立て`draft.js`）を確認したところ、どちらも見た目・動作を変えない安全な変更だったため直接マージした。
- 続けてApp.js内でまだ切り出されていない純粋ロジックを探し、ギフトページ（`GiftModal`）の中にあった「公開中で未達成のアイテムだけ絞り込む処理」と「共有シートに渡す本文（箇条書き）を組み立てる処理」を発見。新しく`gift.js`に`giftableItems()`・`giftShareMessage()`として切り出し、テストを4件追加した（新規PR #67）。
- 結果：`npx jest` は 21 suites / 210 tests すべて成功（作業開始時は196 tests）。`gift.js` は statements/branches/functions/lines すべて100%。
- App.js以外の純粋ロジック抽出は、探すたびに小さな候補が見つかる状態が続いている。次回以降も同じ探し方を続けつつ、そろそろ新機能系PR（特に#63のキーワード検索、MVP仕様の必須項目）をオーナーに実機確認してもらいたい。

## 2026-08-28（今回）

- 前回（8/27）の整理PR #67（`gift.js` へのギフトページロジック分離）はまだ確認・マージ待ちのまま残っている。
- App.js内でまだ切り出されていない純粋ロジックを探し、通知タブ（`NotifyTab`）の中にあった「未達成かつ通知ONのアイテムを絞り込み、次に思い出す時刻順に並べ、今日/今週/それ以降に振り分ける」処理を発見。`notify.js`に`groupNotifyItems()`として切り出し、テストを6件追加した（新規PR #68）。
- 結果：`npx jest` は 20 suites / 212 tests すべて成功（作業開始時は206 tests）。`notify.js` は statements/branches/functions/lines すべて100%。
- App.js以外の純粋ロジック抽出は、探すたびにまだ小さな候補が見つかる状態。次回以降も同じ探し方を続けつつ、#63（キーワード検索、MVP仕様の必須項目）を含む未マージの新機能PRをオーナーに実機確認してもらいたい。

## 2026-08-29（今回）

- 前回（8/28）までに溜まっていた整理PR2件（#67 `gift.js`へのギフトページロジック分離、#68 `notify.js`への通知振り分けロジック分離）を確認したところ、どちらも見た目・動作を変えない安全な変更だったため直接マージした。#68は同じ日付の見出しで#67とdocsファイルが衝突していたため、手動で解消した（内容はどちらも維持し、日付順に並べ直しただけ）。
- 続けてApp.js内でまだ切り出されていない純粋ロジックを探し、ホーム画面の手動並べ替え（`reorderItems`）の中にあった「未達成カードを指定順に並べ、達成済みは末尾に保持する」処理を発見。既存の`reorder.js`に`reorderedItems()`として切り出し、テストを4件追加した（新規PR）。
- 結果：`npx jest --coverage` は 21 suites / 220 tests すべて成功（作業開始時は212 tests）。`reorder.js` は statements/branches/functions/lines すべて100%。
- App.js以外の純粋ロジック抽出は、探すたびにまだ小さな候補が見つかる状態が続いている。次回以降も同じ探し方を続けつつ、#63（キーワード検索、MVP仕様の必須項目）を含む未マージの新機能PRをオーナーに実機確認してもらいたい。

## 2026-08-30（今回）

- ビジョンボード（欲求のストック）を、以前からの積み残し課題だった新データモデルへ刷新する作業を、この日の中で複数回に分けて進めた（コミット7件）。
  - カテゴリ（種別）→ビジョン（1つの願い）の2階層に整理し、ステータスを「叶えたい／叶えている最中／叶った」の3段階に統一（`vision.js`のv1→v2移行関数`migrateVision`つき、保存データは自動で移行）。
  - 画面構成を「上=画像を主役にしたPinterest風2カラム表示／下=タグ段の絞り込み」に整理し、確認画面・完了演出（ホールドして完了、日数表示）まで一通り作り込んだ。
  - ヘッダーのアイコンの役割を整理（タグ=絞り込み、トロフィー=叶った夢一覧、並べ替えアイコン=ソート）。
- 実装が一段落したところでテストと構文を確認：`npx jest` は 21 suites / 226 tests すべて成功。App.jsの構文もbabelで問題なし（既存の無関係な行でCLI単体実行時にのみ出る警告は確認済み・今回の変更とは無関係）。
- これまでこの一連の変更はPRを作らずコミットのみ進んでいたため、今回のセッションでPRを作成した（見た目・操作感が大きく変わる機能変更のため、直接マージはせずオーナーの実機確認待ちとする）。

## 2026-08-31（今回）

- 前回（8/30）までに溜まっていた整理PR2件（#69 `reorderItems`の`reorder.js`への分離、#70 ビジョンボードv2刷新の記録用docs更新）を確認したところ、どちらも見た目・動作を変えない安全な変更だったため直接マージした。両PRとも同じ日付見出しでdocsファイルに衝突していたため、内容はどちらも残したまま日付順に並べ直して手動解消した。
- 続けてApp.js内を見直し、`fmtYMD(ms)`という日付整形の関数がどこからも呼ばれていない死んだコードであることを発見（2026-08-21に見つけた`browserUrl`と同じパターン）。App.jsから削除した（新規PR）。
- 結果：`npx jest` は 21 suites / 230 tests すべて成功（削除前と同数。もともとテストの無い未使用コードだったため）。App.jsの構文もbabelで確認済み。
- App.js以外の純粋ロジック抽出はほぼ出尽くしており、今回は死んだコードの掃除のみ。次回は改めてApp.js内の未使用コード・未テスト分岐を探しつつ、引き続き#63（キーワード検索、MVP仕様の必須項目）を含む未マージの新機能PRをオーナーに実機確認してもらいたい。

## 残っている未マージPR（オーナーの判断が必要）

新機能・UI変更を含むため、動作確認や好みの判断が要る。実機で試してから、採用するものだけマージしてください。

| PR | 内容 | 備考 |
|---|---|---|
| #2 | データにspaceId（将来の共有機能向け下ごしらえ） | 見た目の変化なし。trunkから最も離れているため要リベース確認 |
| #5 | マイページに「振り返り(Wrapped)」カードを追加 | 新しいモーダルUIが増える |
| #20 | 保存シートに「スクショの文字から自動入力」欄を追加 | #32と役割が近い（テキスト→カテゴリ推測）。UIまで含めて完成しており実利用しやすそう |
| #24 | ホームに「写真なし」フィルタチップを追加 | |
| #26, #37, #38 | 詳細画面から後付けでリンクの画像/カテゴリを取り込む機能（3案） | **同じ課題への3通りの実装。中身を見てどれか1つだけ選んでください**（#26は最も古くtrunkとズレている可能性、#37は「リンクを紐付ける」、#38は「写真だけ後付け」で作りが少し違う） |
| #63 | ホームにキーワード検索バーを追加 | MVP仕様書で必須項目とされていたもの（旧#30をtrunk最新版に作り直したもの。#30はクローズ済み）。優先して見てもらいたい |
| #32 | タイトル入力からカテゴリを自動推測（貼り付け欄UIは無し） | #20と役割が近い |
| #48 | 写真のGPS情報から「撮影場所を地図で見る」アクションを追加 | 新しいアクション導線が増える |

## 2026-09-02（今回）

- 前回（8/31）までに溜まっていた整理PR #72（ビジョンタブの絞り込み・並べ替え・セクション化ロジックを`vision.js`へ分離）を確認したところ、見た目・動作を変えない安全な変更だったため直接マージした。
- 続けて`vision.js`内を見直し、`buildVisionBoard()`という関数がApp.jsのどこからも呼ばれていない死んだコードであることを発見（2026-08-21の`browserUrl`、2026-08-31の`fmtYMD`と同じパターン）。以前のビジョンボード画面（旧デザイン）で使われていたが、8/30の新デザイン刷新（`buildVisionTabView`ベースの一覧表示）で置き換えられ、呼び出し元が消えた後も関数とテストだけが残っていたもの。関数本体と対応するテスト4件を削除した（見た目・動作の変化なし）。
- 結果：`npx jest --coverage` は 21 suites / 231 tests すべて成功（作業開始時は235 tests。使われていない関数のテストを削除したため件数が減少）。`App.js`の構文もbabelで確認済み。
- なお、同じくビジョンの並べ替え関連で、App.js内の`reorderVision()`関数と`VisionTab`に渡されている`onReorder`propも、新デザインでは呼び出されていない（並べ替えUIが「新しい順/古い順」のソートに置き換わったため）ように見えたが、`App.js`本体の変更は影響範囲の確認により時間がかかるため今回は見送り、次回以降の調査候補として残す。

## 2026-09-03（今回）

- 前回（9/2）の整理PR #73（`vision.js`内の未使用関数`buildVisionBoard()`削除）を確認したところ、見た目・動作を変えない安全な変更だったため直接マージした。
- 続けて、そのPRで次回調査候補として残されていた`reorderVision()`（App.js内）と、`VisionTab`に渡されている`onReorder`propを調査。`VisionTab`の中身を確認したところ、`onReorder`は関数の引数として受け取っているだけで、本文のどこからも呼び出されていないことを確認した（並べ替えUIが「新しい順/古い順」のソートに置き換わり、ドラッグ並べ替え自体がビジョン画面から無くなっていたため）。呼び出し元の無い`reorderVision()`関数本体と、`onReorder`propの受け渡し（呼び出し側・`VisionTab`の引数）をApp.jsから削除した（見た目・動作の変化なし。ホーム画面の並べ替え`onReorder={reorderItems}`は別物で影響なし）。
- 結果：`npx jest --coverage` は 21 suites / 231 tests すべて成功（削除前と同数。もともとテストの無い未使用コードだったため）。App.jsの構文もbabelで確認済み（CLI単体実行時のみ出る無関係な既知の警告は変更前から存在することを確認済み）。
- App.js以外の純粋ロジック抽出・死んだコード掃除はほぼ出尽くしており、次回は改めてApp.js内の未使用コード・未テスト分岐を探しつつ、引き続き#63（キーワード検索、MVP仕様の必須項目）を含む未マージの新機能PRをオーナーに実機確認してもらいたい。

## 2026-09-04（今回）

- 前回（9/3）の整理PR #74（ビジョン画面の未使用`reorderVision()`と`onReorder`prop削除）を確認したところ、見た目・動作を変えない安全な変更だったため直接マージした。
- 続けてApp.js内でまだ切り出されていない純粋ロジックを探し、通知タイミング編集UI（`ReminderEditor`）の中にあった「通知の種類を選び直したときに、日時未設定なら既定値（翌日9:00）も一緒に入れる」判定と、日時／時刻ピッカーに表示する初期値の計算を発見。`notify.js`に`defaultReminderAt()`・`reminderChoicePatch()`・`reminderAtPickerMs()`・`reminderTimePickerMs()`として切り出し、テストを14件追加した（新規PR）。
- 結果：`npx jest --coverage` は 21 suites / 245 tests すべて成功（作業開始時は231 tests）。`notify.js`は statements/branches/functions/lines すべて100%。App.jsの構文も babel（プロジェクトの`babel.config.js`経由）で確認済み。
- App.js以外の純粋ロジック抽出は、探すたびにまだ小さな候補が見つかる状態が続いている。次回以降も同じ探し方を続けつつ、引き続き#63（キーワード検索、MVP仕様の必須項目）を含む未マージの新機能PRをオーナーに実機確認してもらいたい。

## 2026-09-05（今回）

- 前回（9/4）の整理PR #75（通知タイミング編集の日時計算ロジックを`notify.js`へ分離）を確認したところ、見た目・動作を変えない安全な変更だったため直接マージした。
- 続けてApp.js内でまだ切り出されていない純粋ロジックを探し、ビジョン新規追加（`addVision`）の中にあった「既存一覧の最大order+1を採番し、入力値から新規ビジョンのデータを組み立てる」処理を発見。`vision.js`に`buildNewVision()`として切り出し、テストを4件追加した（新規PR）。
- 結果：`npx jest --coverage` は 21 suites / 249 tests すべて成功（作業開始時は245 tests）。App.jsの構文もbabel（プロジェクトの`babel.config.js`経由）で確認済み。
- App.js以外の純粋ロジック抽出は、探すたびにまだ小さな候補が見つかる状態が続いている。次回以降も同じ探し方を続けつつ、引き続き#63（キーワード検索、MVP仕様の必須項目）を含む未マージの新機能PRをオーナーに実機確認してもらいたい。

## 2026-09-06（今回）

- 前回（9/5）の整理PR #76（ビジョン新規追加の組み立てロジックを`vision.js`へ分離）を確認したところ、見た目・動作を変えない安全な変更だったため直接マージした。
- 続けてApp.js内でまだ切り出されていない純粋ロジックを探し、ホーム画面のアイテム新規追加（`addItem`）の中にあった「保存フォームの入力値から、保存用アイテムのデータを組み立てる」処理を発見。既存の`draft.js`（保存データの組み立てを集めたファイル）に`buildNewItem(data, now)`として切り出し、テストを3件追加した（新規PR）。
- 結果：`npx jest --coverage` は 21 suites / 252 tests すべて成功（作業開始時は249 tests）。`draft.js`は statements/branches/functions/lines すべて100%。App.jsの構文も babel（プロジェクトの`babel.config.js`経由、`@babel/core`のtransformFileSyncで確認。`npx babel`のCLI直接実行は無関係な既知のエラーが変更前から出る）。
- App.js以外の純粋ロジック抽出は、探すたびにまだ小さな候補が見つかる状態が続いている。次回以降も同じ探し方を続けつつ、引き続き#63（キーワード検索、MVP仕様の必須項目）を含む未マージの新機能PRをオーナーに実機確認してもらいたい。

## 2026-09-07（今回）

- 前回（9/6）の整理PR #77（アイテム新規追加の組み立てロジックを`draft.js`へ分離）を確認したところ、見た目・動作を変えない安全な変更だったため直接マージした。
- 続けてApp.js内でまだ切り出されていない純粋ロジックを探し、箱庭（`waterPlant`）の中にあった「今日あと何回水やりできるかを見て、可能なら成長ポイント・水やり回数を更新した新しい状態を作る」処理を発見。既存の`garden.js`に`waterGarden(state, now)`として切り出し、テストを6件追加した（新規PR）。
- 結果：`npx jest --coverage` は 21 suites / 258 tests すべて成功（作業開始時は252 tests）。`garden.js`はstatements/branches/functions/lines ほぼ100%（残り1分岐は2026-08-21から到達不可能と判断済みの防御的コードで対象外）。App.jsの構文もbabel（プロジェクトの`babel.config.js`経由、`@babel/core`のtransformで確認）。
- App.js以外の純粋ロジック抽出は、探すたびにまだ小さな候補が見つかる状態が続いている。次回以降も同じ探し方を続けつつ、引き続き#63（キーワード検索、MVP仕様の必須項目）を含む未マージの新機能PRをオーナーに実機確認してもらいたい。

## 2026-09-09（今回）

- 前回（9/7）の整理PR #78（箱庭の水やり処理を`garden.js`へ分離）を確認したところ、見た目・動作を変えない安全な変更だったため直接マージした。
- 続けてApp.js内でまだ切り出されていない純粋ロジックを探し、カテゴリ管理（`addCategory`）の中にあった「新規カテゴリのid・名前・色を組み立てる」処理を発見。`vision.js`に`buildNewCategory(name, color, now)`として切り出し、テストを5件追加した（新規PR）。
- 結果：`npx jest --coverage` は 21 suites / 263 tests すべて成功（作業開始時は258 tests）。App.jsの構文もbabel（プロジェクトの`babel.config.js`経由、`@babel/core`のtransformFileSyncで確認）。
- App.js以外の純粋ロジック抽出は、探すたびにまだ小さな候補が見つかる状態が続いている。次回以降も同じ探し方を続けつつ、引き続き#63（キーワード検索、MVP仕様の必須項目）を含む未マージの新機能PRをオーナーに実機確認してもらいたい。

## 次にやること

- オーナーが上記表を見て、欲しい機能のPRだけ実機（Expo Go）で試してマージ。同じ課題の複数案（#26/#37/#38、#20/#32）はどちらか一方を選んでもう片方をクローズ。
- 特に #63（キーワード検索、旧#30）はMVP仕様の必須項目のまま1ヶ月以上未マージなので、優先して見てもらいたい。
- ビジョンボード（欲求のストック）の新デザイン・新データモデルは、trunk（`claude/upbeat-bohr-1y69a2`）に既に取り込み済み。次に実機（Expo Go）を開いたときに、見た目・操作感（特に「ホールドして完了」の長押し操作）に違和感が無いか確認してほしい。
- 以降の自動セッションは、この一覧にある機能と重複する新規PRを作らないようにする（作り始める前に必ずこのファイルを確認する）。
