# 未マージPRの整理状況（2026-07-29）

これまでの自動セッションで「壊れたテストの修正」を提案するPRが繰り返し作られ、GitHubに未マージPRが積み上がっていた。今回、機械的に安全と判断できるものだけを直接マージ／クローズし、製品判断が必要なものは手つかずのまま残した。次にオーナーが見るときに、まとめて判断できるようにこの記録を残す。

## 今回やったこと

- **マージ**：#39（`helpers.test.js` をカテゴリ既定値『未設定』の仕様に合わせる修正）→ trunk (`claude/upbeat-bohr-1y69a2`) の壊れていたテストが直った。
- **マージ**：#7（`links.js` にテストを追加。既存コードの変更なし、新規テストファイルのみ）。
- **クローズ（重複）**：#34, #35, #36 … #39と全く同じ内容の修正だったため。
- 上記の結果、`npx jest` は 12 suites / 101 tests すべて成功（作業前は1件失敗）。

## 追記（2026-07-31）

- **マージ**：#41（マイページの日付集計ロジックをstats.jsに分離。trunkに合わせた作り直し版）。見た目・挙動の変更なし、テスト追加のみの安全な整理だったため、判断不要と判断して直接マージ。
- **クローズ（重複）**：#3 … #41と全く同じ目的（stats.js分離）で、#41が現行trunkに合わせて作り直した版だったため。
- **#2はクローズしていません**：#41のPR本文は「#2, #3はこのPRとかぶる」としていましたが、確認したところ#2は別機能（spaceId付与）で内容が重ならないため、そのまま残しました。ただしtrunkから最も離れており(6週間前が起点)、リベース確認は要ります。
- 今回、あらためて`app/textParse.js`という名前でテキスト→カテゴリ推測の3つ目の実装を作りかけましたが、着手前にこの一覧を確認して#20・#32と重複すると気づき、PRを作らずに破棄しました。**#20と#32はロジックがほぼ同じ**（キーワード一致でカテゴリ推測）で、#20は貼り付け欄のUIも追加している分、実際に今すぐ使える完成度が高そうです。オーナーが実機で確認するときの参考にしてください。
- 上記のとおり`npx jest`は全13スイート・108件が成功する状態を保っています。

## 残っている未マージPR（オーナーの判断が必要）

新機能・UI変更を含むため、動作確認や好みの判断が要る。実機で試してから、採用するものだけマージしてください。

| PR | 内容 | 備考 |
|---|---|---|
| #2 | データにspaceId（将来の共有機能向け下ごしらえ） | 見た目の変化なし。trunkから最も離れているため要リベース確認 |
| #5 | マイページに「振り返り(Wrapped)」カードを追加 | 新しいモーダルUIが増える |
| #20 | 保存シートに「スクショの文字から自動入力」欄を追加 | #32と役割が近い（テキスト→カテゴリ推測）。UIまで含めて完成しており実利用しやすそう |
| #24 | ホームに「写真なし」フィルタチップを追加 | |
| #26, #37, #38 | 詳細画面から後付けでリンクの画像/カテゴリを取り込む機能（3案） | **同じ課題への3通りの実装。中身を見てどれか1つだけ選んでください**（#26は最も古くtrunkとズレている可能性、#37は「リンクを紐付ける」、#38は「写真だけ後付け」で作りが少し違う） |
| #30 | ホームにキーワード検索バーを追加 | MVP仕様書で必須項目とされていたもの |
| #32 | タイトル入力からカテゴリを自動推測（貼り付け欄UIは無し） | #20と役割が近い |

## 次にやること

- オーナーが上記表を見て、欲しい機能のPRだけ実機（Expo Go）で試してマージ。同じ課題の複数案（#26/#37/#38、#20/#32）はどちらか一方を選んでもう片方をクローズ。
- 以降の自動セッションは、この一覧にある機能と重複する新規PRを作らないようにする（今回のように、作り始める前に必ずこのファイルを確認する）。
